import React from 'react';
import { Typography } from 'antd';

const TypesPage: React.FC = () => {
    return (
        <div style={{ padding: '20px' }}>
            <Typography.Title level={3}>Types</Typography.Title>

        </div>
    );
};

export default TypesPage;
